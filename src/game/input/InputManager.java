package game.input;

/*
CLASS: InputManager
DESCRIPTION: coordinates all input handling by managing mouse and keyboard
             events and distributing them to appropriate handlers
*/

class InputManager {

    //anonymous class example for key event listener
    void setupKeyListener() {
        /*
        KeyListener listener = new KeyListener() {
            public void keyPressed(KeyEvent e) {
                //handle key press
            }
            public void keyReleased(KeyEvent e) {
                //handle key release
            }
            public void keyTyped(KeyEvent e) {
                //handle key typed
            }
        };
        */
    }

}